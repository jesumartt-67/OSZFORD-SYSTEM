## 4. Estrategia de Trabajo en Equipo (Git Flow Simplificado)

Para que los 3 integrantes no mezclen sus cables (conflictos de código), usaremos **Ramas (Branches)**. Nadie debe trabajar directamente sobre `main`.

### Protocolo para cada integrante:

Cada vez que un integrante inicie una tarea (ejemplo: Crear el modelo de base de datos), debe seguir estos 3 pasos:

4.1.  **Sincronizar:** Traer lo último del equipo.
    `git pull origin main`
4.2.  **Crear Rama:** Crear un espacio de trabajo propio "Una rama = Una tarea (o funcionalidad)". O sea, una rama por cada Tarea.
    `git checkout -b feature/jesus-nombre-de-la-tarea`
	
	***NOTA:*** aqui la rama se llama feature/nombre-de-la-tarea
	
	4.2.1.	La Anatomía del Nombre
			feature/: Es un prefijo o "carpeta virtual". Se usa para organizar las ramas por su propósito. Es un estándar en la industria (Git Flow).
			nombre-de-la-tarea: Es el identificador específico de lo que estás haciendo (ej: registro-vehiculo, api-simit, login-colaborador).
	4.2.2. ¿Por qué usar el prefijo feature/?
			En herramientas como GitHub o tu propio Antigravity IDE, usar carpetas (prefijos) ayuda a mantener el orden:
			feature/: Para nuevas funcionalidades (lo que más usarán ahora).
			bugfix/: Para corregir errores que encuentren en el camino.
			docs/: Para cuando alguien solo esté redactando manuales o documentación.
	4.2.3.	Ejemplos Reales para tu Equipo
			Si los 3 integrantes se reparten el trabajo inicial, así deberían crear sus ramas:

			Integrante 1 (Base de Datos):
				git checkout -b feature/jm-setup-db-mysql
			Integrante 2 (Backend/FastAPI):
				git checkout -b feature/kr-api-inspeccion-motos
			Integrante 3 (Frontend/Interfaz):
				git checkout -b feature/as-ui-formulario-preoperacional	
	
4.3.  **Subir Cambios:** Al terminar, subir su rama.
    `git add .`
    `git commit -m "Descripción clara de lo que hiciste"`
    `git push origin feature/nombre-de-la-tarea` 


*** El Paso a Paso Final para Limpiar Todo ***
Para que tanto la rama de la tarea como la main queden idénticas a lo que tienes en pantalla, ejecuta esta secuencia en tu PowerShell:

1. Consolidar la rama de la tarea (feature/jesus-tarea)
PowerShell
# Asegúrate de estar en la rama de la tarea
git checkout feature/jesus-tarea

# Paso A: "Invitar" a los archivos nuevos (las 'U') y registrar los cambios (la 'M')
git add .

# Paso B: Guardar la fotografía final
git commit -m "feat: descripcion"

# Paso C: Subir esta rama específica a GitHub
git push origin feature/jesus-tarea
2. Actualizar la rama Principal (main)
PowerShell
# Paso D: Cambiar a la rama que ven tus compañeros
git checkout main

# Paso E: Traer todo el trabajo de la rama de Jesús a la principal
git merge feature/jesus-tarea

# Paso F: Subir el resultado final a la nube
git push origin main
¿Cómo saber si funcionó?
Una vez que termines el Paso F, las letras U y M en tu VS Code deberían desaparecer. Eso significa que tu copia local y la de la nube están perfectamente "espejadas".


----------------------------
1. Crear la nueva rama y moverte a ella
Estando en tu terminal de Vagrant (dentro de la carpeta del proyecto), crea la rama para la nueva tarea:
----------------------
Bash
---------------------
vagrant@ubuntu-focal:~/OSZFORD-SYSTEM$ git checkout -b feature/aut-multifactor-y-model
Switched to a new branch 'feature/aut-multifactor-y-model'
vagrant@ubuntu-focal:~/OSZFORD-SYSTEM$
-----------------------
Esto crea la rama y te posiciona en ella automáticamente.
2. Guardar y subir el trabajo a esa rama
Ahora vamos a registrar lo que ya hiciste en esta nueva rama:
Agrega los cambios:
-----------------------
Bash
-----------------------
vagrant@ubuntu-focal:~/OSZFORD-SYSTEM$ git add .
vagrant@ubuntu-focal:~/OSZFORD-SYSTEM$ 
----------------------
3. Crea el commit:
-------------------------
Bash
--------------------------
vagrant@ubuntu-focal:~/OSZFORD-SYSTEM$ git commit -m "Implementación inicial de Autenticación Multifactor y Modelos"
[feature/aut-multifactor-y-model 72c23c2] Implementación inicial de Autenticación Multifactor y Modelos
 22 files changed, 1227 insertions(+), 312 deletions(-)
 create mode 100644 backend/__init__.py
 create mode 100644 backend/__pycache__/__init__.cpython-38.pyc
 ...
 create mode 100644 docs/PROMPT HACKATON OSZFORD - PASO D.txt
vagrant@ubuntu-focal:~/OSZFORD-SYSTEM$ 
------------------------
4. Sube la rama al servidor (GitHub):
--------------------------
Bash
--------------------------
vagrant@ubuntu-focal:~/OSZFORD-SYSTEM$ git push origin feature/aut-multifactor-y-model
Username for 'https://github.com': jesumartt
Password for 'https://jesumartt@github.com': <encuentre el pass en el archivo 'tokes del proyecto.txt'>
Enumerating objects: 40, done.
...
To https://github.com/jesumartt-67/OSZFORD-SYSTEM.git
 * [new branch]      feature/aut-multifactor-y-model -> feature/aut-multifactor-y-model
vagrant@ubuntu-focal:~/OSZFORD-SYSTEM$ git push origin feature/aut-multifactor-y-model
---------------------------
1. Actualizar la rama MAIN
Ahora que el respaldo de la tarea está en la nube, vamos a "unificar" todo en la rama principal para tus compañeros:
2. Regresa a main:
-----------------
Bash
-----------------
vagrant@ubuntu-focal:~/OSZFORD-SYSTEM$ git checkout main
----------------
Trae los cambios de la tarea al main:
---------------------
Bash
----------------------
vagrant@ubuntu-focal:~/OSZFORD-SYSTEM$ git merge feature/aut-multifactor-y-model
------------------------
Sube el main definitivo a GitHub:
------------------------
Bash
vagrant@ubuntu-focal:~/OSZFORD-SYSTEM$ git push origin main
------------------------

Si todo se ve correcto, termina la secuencia así:
Como ya estamos en la rama de la tarea, dale el empujón final al repositorio:

Asegura los archivos:

Bash
git add .
Crea el commit:

Bash
git commit -m "Estructura final de modelos y autenticación para el equipo"
Sube tu rama de trabajo:

Bash
git push origin feature/aut-multifactor-y-model
Pásalo a main:

Bash
git checkout main
git merge feature/aut-multifactor-y-model
git push origin main
El link para tus compañeros:
Una vez que termines el último push origin main, diles que este es el repositorio oficial:
https://github.com/jesumartt-67/OSZFORD-SYSTEM
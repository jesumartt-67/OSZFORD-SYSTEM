ejecutar la prueba visual en Swagger UI:
# Entrar a la VM si no estás en ella
vagrant ssh
cd ~/OSZFORD-SYSTEM

# Iniciar el servidor FastAPI con recarga automática
uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload

Abre tu navegador en Windows y ve a: http://localhost:8000/docs